# Relicblade Deckbuilder
 
# File Structure.
/dist, contains the front-end
/server, contains the lambda that runs the backend

To sync a paticular app, use `cap sync`. Then you can run `cap open` to open the correct one.